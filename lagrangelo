#!/usr/bin/env bash
# Start the Flask app via gunicorn. Reads PORT env or first arg, defaults to 5000.
PORT="${PORT:-$1}"
if [ -z "$PORT" ]; then
  PORT=5000
fi

exec gunicorn app:app --bind 0.0.0.0:"$PORT"
